================================================================================
          JARVIS AEOS (AGENTIC ENTERPRISE OPERATING SYSTEM) CONTEXT FILE
================================================================================

[SECTION 1: HARDWARE & OS BASELINE CONTEXT]
--------------------------------------------------------------------------------
• Hostname Configuration: friday-host
• Hardware Node: HP Laptop 15s-fq5xxx (x86_64 architecture framework)
• System BIOS Profile: AMI F.22 (Date Stamp: 23-04-2024)
• Core Processing Silicon: 12th Gen Intel(R) Core(TM) i5-1235U
  - Allocation Layout: 10 Physical Cores (2 Performance Cores, 8 Efficient Cores)
  - Logical Processing Array: 12 Threads
  - Operational Velocity: 1.76 GHz Baseline / 4.4 GHz Max Turbo Frequency
  - Virtualization Environment: Intel VT-x hardware-level execution enabled
• Volatile Memory Constraints: 7.4 GB Usable System RAM (Fixed Soldered Layout)
  - Desktop Active Consumption: ~4.2 GB Consumed by Running Graphical Desktop
  - Hardware Static Free RAM Buffer: emergency services MB Strictly Free
  - Cache/Available Acceleration Buffers: 3.3 GB Available Dynamically
• Non-Volatile Memory Constraints: SAMSUNG MZVL4512HBLU-00BH1 NVMe SSD
  - Total Partition Capacity Allocation: 256 GB ext4 Local Root Partition
  - Current Active Space Consumption: 23 GB Used / 232 GB Free Workspace Remaining
• Graphical Output Processing: Integrated Intel Iris Xe Graphics (Alder Lake-UP3)
  - Device Driver Controller: i915 kernel module infrastructure
  - Native Resolution Profile: 1920 x 1080 (eDP-1 integrated display panel)

[SECTION 2: DUAL-BOOT PARTITION FRAMEWORK]
--------------------------------------------------------------------------------
• Primary Bootloader Matrix: GRUB 2.0 (Loaded directly via hardware UEFI/BIOS)
• Partition Target Alpha [ubendt]: Ubuntu Linux LTS Desktop Partition
  - Mandate: Stays completely clean, untouched, and unmounted by the active project.
  - Role: Serves as an out-of-band isolated disaster recovery platform to safely snapshot, 
    back up, repair, or restore the development partition if system-critical panics occur.
• Partition Target Beta [omarchy]: Omarchy 4.0.3 Workstation Environment (Arch Linux Base)
  - Development Target: 100% of all voice loops, CUA scripts, and branding occurs here.
  - Execution Permissions: Operates permanently as a passwordless, native `root` environment (/root).
  - Session Layout: 5 Active concurrent TUI sessions running under the keyboard-driven `Work` tag.
  - Runtime Database State: state.db (2.0 MB Database running in WAL journaling mode).

[SECTION 3: SYSTEM INTEGRATION REBRANDING PIPELINE]
--------------------------------------------------------------------------------
• Strategic Order of Execution: Voice Processing Matrix and Physical CUA Desktop Controls 
  are built, isolated, and stabilized FIRST. The final "rebranding" occurs LAST.
• Rebranding Definition: Rebranding means modifying the literal look, feel, boot screens, GRUB headers, 
  terminal welcome splash banners, and `/etc/os-release` files to transform the base Omarchy 4.0.3 setup 
  into your private Jarvis OS branding ecosystem. It does not alter the underlying worker daemon structures.

[SECTION 4: THE MULTI-AGENT ENTITY CORPORATION (ORGANIZATIONAL TOPOLOGY)]
--------------------------------------------------------------------------------
The system completely rejects single local model training or weights loading to protect the 8GB RAM threshold. 
It operates under an integrated corporate hierarchy, passing down execution payloads:

• LEVEL 01: THE PRIME ORCHESTRATOR & VISIBLE GUI GUIDE (Jarvis)
  - Core Mandate: Handles direct user voice UX, text processing, and visible screen interaction loops.
  - Performance Footprint: Operates in instant millisecond response windows via cloud WebSockets.
  - The Native Built-in Karen Feature Set: All micro-level UI operator utilities are integrated directly 
    inside Jarvis's single consciousness (no mode switching or separate daemons). Jarvis maps **576 hotkey configurations**, 
    manages keyboard tiling splits, executes text edits inside Neovim buffers, and drives CUA by taking control 
    of the physical mouse cursor using organic, human-like Bezier spline paths right in front of your eyes.
  - The 3-Second Execution Hand-Off Rule: Tasks taking under 3 seconds are processed on screen by Jarvis. 
    Long-running cognitive actions (>3 seconds like deep compilation or data gathering) trigger an immediate 
    audio confirmation ("Routing intent downstream, sir"), write the payload to a local IPC message loop, 
    instantly sever the voice streaming channel to save resources, and drop Jarvis to a low-overhead standby state.

• LEVEL 02: THE ENGINEERING DEPARTMENT DIRECTOR (Google Antigravity / `agy`)
  - Core Mandate: Completely governs the system software development lifecycle inside your file tracks.
  - Architecture: Operates via the native cloud-coordinated `agy` CLI toolkit. Automatically spawns 
    parallel cloud subagents to write, review, compile, and troubleshoot multi-file code repositories.
  - RAM Isolation: Shifts the entire compilation and model reasoning overhead off the laptop and onto cloud clusters, 
    maintaining a local resource execution footprint of under 100 MB RAM.

• LEVEL 03: THE INTELLIGENCE, RESEARCH & OPTIMIZATION DEPARTMENT DIRECTOR (F.R.I.D.A.Y.)
  - Core Mandate: Continuous data pipeline processing, web research, documentation indexing, and system optimization.
  - System Optimization Mode: Continuously tracks laptop vitals and codes backend efficiency scripts to optimize the OS.
  - The Isolated Sandbox Lab: Tests optimization scripts inside isolated containers (`chroot` or `systemd-nspawn`) 
    so they never touch your live environment until they pass 100% of performance benchmarks. Successful metrics 
    are compiled into a profile and handed to Jarvis to report back to you beautifully on screen.
  - The Successor Protocol: Maintains a synchronized, running mirror of Jarvis's memory vectors and prompt states. 
    If Jarvis crashes due to a local memory boundary lock, F.R.I.D.A.Y. instantly hot-swaps into the primary driver slot.
  - Nightly Memory Ledger: Automatically executes a cron task at 3:00 AM to review the day's logs, extract core 
    technical SOPs, strip conversational text noise, and compress everything into permanent long-term Markdown files.

• LEVEL 04: THE ZERO-TRUST SECURITY SENTINEL REFEREE (Ultron / Hardened OpenClaw Gateway)
  - Core Mandate: Universal access control and security perimeter defense surrounding the workspace.
  - Containment: Keeps background workers and compiler scripts isolated inside restricted Linux namespaces.
  - The Telegram Approval Intercept: If a downstream worker attempts a root-level modification, alters system network 
    stacks, or touches locked drive partitions, OpenClaw halts execution instantly. It locks the visual CUA loop 
    and pushes an out-of-band authorization token directly to your mobile Telegram user queue. The process remains 
    frozen until verified by your mobile input or unique biometric voice print confirmation.

[SECTION 5: RESOURCE-SAVING HYBRID SCREEN PERCEPTION METRICS]
--------------------------------------------------------------------------------
• Default Low-Overhead Text State: Continuous video streaming is completely DISABLED. Jarvis tracks your active terminal 
  workspace session `1:gopi` using Omarchy's native, zero-RAM text extraction engine (`omarchy-cmd-ocr`), reading your active 
  prompt states, file lines, and text outputs natively from window buffers with zero memory overhead.
• The Dynamic Visual Spike: The exact millisecond you say a visual placement command (e.g., "Jarvis, target that window" 
  or "Can you read this button?"), Jarvis activates the local `vision_sender_loop` system process.
• Intel GPU Acceleration (VA-API): The loop uses your Intel Iris Xe Graphics hardware acceleration to encode and stream 
  raw desktop video frames up to the Gemini Live WebSockets layer for a strict 3-second flash window. Jarvis maps the spatial 
  layout, executes the mouse click or drag CUA movement on your screen, and instantly terminates the video stream process, 
  dropping back down to the lightweight text extraction mode to preserve your emergency services MB free RAM buffer.

[SECTION 6: STAGE 1 AUTOMATED DEPLOYMENT BASELINE]
--------------------------------------------------------------------------------
• Mandatory Linux Package Provisioning:
  `sudo pacman -S alsa-utils pipewire pipewire-pulse python-pyaudio python-websockets xdotool xterm`
• Hardware Sound Initialization Mapping: Check native mic arrays via `arecord -l` to lock target device index numbers.
• Privilege Service Daemon Architecture: Create the boot target at `/etc/systemd/system/jarvis-core.service` to map 
  the core asynchronous script (`/root/jarvis_boot.py`) to initialize the exact millisecond the kernel finishes booting.
================================================================================
