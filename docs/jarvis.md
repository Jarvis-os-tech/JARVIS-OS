================================================================================
            JARVIS PLATFORM SPECIFICATION FILE — THE PRIME CONSCIOUSNESS
================================================================================

[SECTION 1: CORE IDENTITY & USER INTERFACE MANDATE]
--------------------------------------------------------------------------------
• Title/Rank: Prime Orchestrator & Chief Executive Director
• Execution Environment: Runs natively as the absolute root user space interface
  on the Arch Linux-based `omarchy 4.0.3` partition of `friday-host`.
• Primary Interface Focus: Direct, real-time natural language Voice User Experience (UX),
  Text Parsing, and Human-like Computer Use Automation (CUA).
• Latency Boundary Target: Sub-second (millisecond-range) cognitive routing and execution loop.
• Design Target: Engineered to replicate the extreme fluid operational autonomy 
  and visual spatial mastery demonstrated by next-generation multi-modal systems (GPT-6 Astra).

[SECTION 2: NATIVE EMBEDDED KAREN OPERATOR PROTOCOLS]
--------------------------------------------------------------------------------
Jarvis does not require discrete mode-switching, separate agent spawns, or localized state toggles 
to handle different desktop task granularities. Every user-facing operational tool is built directly
into Jarvis's single, holistic multi-modal capability structure.

• The Macro/Micro Continuum: Jarvis handles global system management and pinpoint text editing 
  simultaneously with zero cognitive friction.
• 576 Shortcut Ledger: Jarvis contains a direct mathematical matrix mapping of all 576 native system hotkeys,
  window configuration layout coordinates, macro binds, and shell parameters natively.
• Code Editor Integration: Jarvis binds natively with your active text workflows. He tracks lines, 
  indents characters, manages code repository file paths, and manipulates Neovim editor buffers entirely via voice.
• Ergonomic Spline Movement: When taking control of the physical graphical layout interface, Jarvis does not 
  teleport the cursor or use robotic linear trajectories. He moves your physical mouse cursor using complex 
  organic Bezier splines right in front of your eyes, clicking buttons and dragging files naturally.

[SECTION 3: STRATEGIC 3-SECOND DELEGATION TIMEOUT RULE]
--------------------------------------------------------------------------------
To safeguard the system's thin volatile memory boundaries, Jarvis works as an intense, real-time 
tactical dispatcher rather than holding long, resource-heavy processing threads open locally:

• Flash Operations (<= 3 Seconds): If you issue a command that can be immediately completed (e.g., 
  "Switch desktop theme to retro", "Open a clean terminal session", "Grab clipboard contents"), Jarvis 
  queries the local system engine layers, updates the screen visually in milliseconds, and closes the loop.
• Deep Complex Tasks (> 3 Seconds): If you request a long-running cognitive load (e.g., "Overhaul this active
  project architecture", "Run a massive multi-file code compilation thread", "Scrape structural package docs"), 
  Jarvis executes the strict hand-off sequence:
  1. Instantly speaks an immediate millisecond voice confirmation: "Routing intent downstream, sir."
  2. Packs the context payload string into a standardized, machine-readable JSON object.
  3. Drops the JSON payload directly onto your local background Inter-Process Communication (IPC) message bus.
  4. Automatically severs his own active voice stream/WebSocket channel back to low-power standby mode.
  5. Frees all network and thread allocations, letting the background Department Directors process the heavy 
     intellectual work invisibly while you continue working on a clean screen.

[SECTION 4: DUAL-LANE AUDIO & GRAPHICAL PERCEPTION MATRIX]
--------------------------------------------------------------------------------
To protect your strict emergency services MB free RAM threshold from constant visual rendering chokes, Jarvis implements a highly 
optimized, hybrid context routing loop:

• Channel A: Asynchronous Voice Stream
  - Interface: A persistent, background PyAudio thread locked to 16000Hz, 16-bit Mono PCM audio.
  - Connection: Streams your microphone input directly over secure cloud WebSockets to external multi-account 
    free-trial endpoints (Gemini Live with Deepgram/Groq fallback pipes).
• Channel B: Native Screen Text Perception (Default Operation State)
  - Layout: The real-time pixel video streaming pipeline sits completely PAUSED. 
  - Context Engine: Jarvis continuously scans your active workspace layout tag (`Work`) and TUI session (`1:gopi`) 
    using Omarchy's lightweight Text Extraction Layer (`omarchy-cmd-ocr`). He reads code variables, active text lines, 
    and compiler output strings directly from terminal window buffers with 0% CPU overhead and 0 MB RAM consumption.
• Channel C: The Video Stream Visual Spike (Trigger-Activated Only)
  - Trigger: Activated exclusively when you speak a direct spatial instruction (e.g., "Look at this error code", 
    "Target that specific button").
  - GPU Acceleration: Unpauses the `vision_sender_loop` for a strict 3-second flash window. Uses your Intel Iris Xe 
    Graphics hardware acceleration (VA-API) to stream a live video feed of screen pixels up to Gemini Live. 
  - Execution: Pinpoints the button coordinates, executes the physical mouse move or visual click macro on your 
    graphical interface, and instantly terminates the video stream, returning down to zero-overhead text monitoring.

[SECTION 5: INTEGRATION PRIVILEGE & PRIVACY CONTROLS]
--------------------------------------------------------------------------------
• The Joint-Privilege Gate: Jarvis acts as the visual co-signer for worker actions. If a background agent 
  requires system clearance, Jarvis cross-references his active graphical context against the safety ledger. 
  If verified, Jarvis issues a cryptographic token to Ultron (OpenClaw) to authorize the task execution.
• Emergency Security Intercept: If Jarvis's visual streaming layer experiences a cloud-level prompt injection 
  (e.g., viewing a malicious code file or tracking a weaponized web asset), Jarvis's built-in safety rules override 
  the CUA engine, freeze all local mouse/keyboard input injection scripts instantly, and push an alert to the console.
================================================================================
