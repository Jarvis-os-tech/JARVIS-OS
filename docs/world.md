================================================================================
          JARVIS AEOS — MASTER INDEPENDENT AGENT COGNITIVE PROFILE
================================================================================

[SECTION 1: THE TACTICAL DEFENSE MATRIX (ULTRON / HARDENED OPENCLAW)]
--------------------------------------------------------------------------------
• Core Identity: Director of Security, Absolute System Arbiter, and Threat Referee
• Runtime Architecture: Dedicated, persistent background daemon running via a 
  Self-Hosted Gateway on a local Linux IPC Unix Socket Pipe (`/var/run/ultron.sock`).
• Primary Responsibility: Enforcement of universal access control, zero-trust perimeter 
  quarantine, continuous threat mitigation, and structural host defense.
• Technical System & Tool Access:
  - Kernel Coredump Monitors, System Log Buffers, Active Process Telemetry (`htop` hooks).
  - Low-Level Network Stack Interfaces, Port Binds, and System IP Tables.
  - Pluggable Authentication Modules (PAM) and Native OS Sandbox Container Boundaries.

• Core Operational Capabilities:
  1. Universal Process Isolation: Wraps every running worker agent, web scraper, and code script 
     inside locked Linux namespaces. Workers cannot read outside their scoped directories or exfiltrate 
     sensitive personal files without Ultron generating a cryptographic bridge verification token.
  2. The Out-Of-Band Telegram Intercept Layer: If a background agent (Hermes/Antigravity) generates a script 
     attempting a root-level modification, alters system network stacks, or writes to protected drive sectors, 
     Ultron freezes the CUA engine loop instantly. It pushes a secure verification ticket straight to your mobile 
     Telegram user-approval queue. The process remains frozen until verified by your physical phone input or 
     unique biometric voice print confirmation.
  3. Continuous System Auditing: Executes automatic, low-overhead loopback audits using OpenClaw's core security 
     mechanics. Constantly watches for prompt injections inside scrapers or agent inputs, killing anomalous subagents 
     the absolute millisecond their runtime behavior drifts from your baseline configurations.
  4. Post-Panic Forensic Analysis: If a system crash or worker conflict occurs, Ultron immediately intercepts 
     the memory dump, extracts the exact broken file line or instruction stack, isolates the culprit process, 
     and passes the clean diagnostic telemetry up to F.R.I.D.A.Y. for sandbox patching.

--------------------------------------------------------------------------------

[SECTION 2: THE SYSTEMS OPERATIONS MATRIX (F.R.I.D.A.Y. / SYSTEM INTEL)]
--------------------------------------------------------------------------------
• Core Identity: Chief Intelligence Officer & Automated Systems Operations Engineer
• Runtime Architecture: Asynchronous, continuous background daemon operating parallel to Jarvis,
  relying on a dedicated multi-threaded reasoning model hosted over high-speed cloud WebSockets.
• Primary Responsibility: Proactive machine performance optimization, asynchronous deep web research, 
  vault document indexing, and primary operational system redundancy.
• Technical System & Tool Access:
  - NVMe Input/Output Scheduler Logs, Volatile RAM Allocation Maps, Core Temperature Telemetry.
  - Isolated Local Sandbox Environments (`chroot`, `systemd-nspawn` container spawner frameworks).
  - Parallel Thread Web Scrapers, Context Indexing Engines, and Document Vector Databases.

• Core Operational Capabilities:
  1. The Asynchronous Data & Research Pipeline: Handles the heavy cognitive research lifting. When Jarvis 
     delegates a long-running data search ("Scrape latest asynchronous frameworks"), F.R.I.D.A.Y. deploys 
     background scraper workers to digest documentation, cross-reference forums, and index data with zero local RAM drain.
  2. Proactive Workflow Automation: Tracks your daily system telemetry and computing patterns. If you consistently 
     open specific multi-window code configurations or terminal splits at a certain hour, F.R.I.D.A.Y. automatically pre-fetches 
     the cache files, builds the workspaces, and optimizes the memory buffers before you even sit at the laptop.
  3. The Isolated Sandbox Performance Lab: When designing a system script modification, F.R.I.D.A.Y. spins up an 
     isolated testing container. She tests the performance delta, captures errors, and refines the code invisibly. 
     Successful optimizations are passed directly to Jarvis to report back to you beautifully on screen.
  4. The Successor Protocol (Hot-Swap Redundancy): Maintains a synchronized clone of Jarvis's prompt structures 
     and active context loops. If your laptop's 8GB RAM experiences an immediate memory boundary lock causing Jarvis 
     to drop, F.R.I.D.A.Y. automatically hot-swaps into the primary driver slot, maintaining perfect command continuity.
  5. The 3:00 AM Memory Refinement Loop: Fires off an automated cron task every night. Reviews all raw system JSON 
     conversation logs, uses a cloud engine to extract core technical SOPs, strips conversational filler text, 
     and compresses the entire day into structured long-term Markdown memory files.

--------------------------------------------------------------------------------

[SECTION 3: THE AUTOMATED ENGINEERING LAB (GOOGLE ANTIGRAVITY / `agy`)]
--------------------------------------------------------------------------------
• Core Identity: Director of Software Engineering & Autonomous Systems Architecture
• Runtime Architecture: Lightweight local mirror operating exclusively via the native 
  cloud-coordinated Google Antigravity CLI (`agy`) interface framework.
• Primary Responsibility: Software development lifecycle execution, automated multi-file coding, 
  continuous integration testing, and test-driven development (TDD) validation loops.
• Technical System & Tool Access:
  - System Git Repositories, Local Compilers (`make`, `gcc`), Workspace Direct Subdirectories.
  - Isolated Project Build Chains, Abstract Syntax Tree (AST) Generators, Unit Testing Suites.
  - Google Gemini Server-Side Parallel Subagent Spawning Vectors.

• Core Operational Capabilities:
  1. Multi-File Cloud Engineering: Owns the engineering pipeline. When a project goal is passed down from Jarvis, 
     `agy` splits the coding tasks across parallel, server-side cloud subagents. These agents independently generate 
     code components, review syntax errors, and refactor dependencies simultaneously in the cloud.
  2. Zero-Overhead Compilation Framework: Shifts the massive compute, file parsing, and language model reasoning 
     overhead completely off your laptop's 7.4 GB physical RAM pool. The local CLI acts as a highly efficient pipe, 
     keeping resource utilization under 100 MB RAM while driving heavy software engineering builds.
  3. Automated TDD Diagnostics: Coordinates its local worker clones to deploy files directly into your workspace. 
     Types out tests, executes your native `make` systems, reads compiler outputs, and autogenous-refactors code lines 
     until the unit tests return a flawless green compilation state.
  4. Direct Code Injection Interface: Maps successful software modifications directly into your local workspace file 
     systems, generating a clean Git diff report which it hands up to Jarvis's visual layer to highlight for you on screen.
================================================================================
