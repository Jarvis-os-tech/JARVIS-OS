# JARVIS-OS Hermes External Memory Provider Configs
# One per department profile — place in ~/.hermes/profiles/<dept>/config.yaml
# or reference via HERMES_EXTERNAL_MEMORY_PROVIDER env var

# ============================================================
# RESEARCH PROFILE
# ============================================================
research:
  external_memory:
    provider: "obsidian"
    vault_path: "/home/g0pi/Projects/Jarvis-OS/memory/vault/research"
    read_only: false
    scope: "department"
    index_file: "_index.json"

# ============================================================
# CODER PROFILE
# ============================================================
coder:
  external_memory:
    provider: "obsidian"
    vault_path: "/home/g0pi/Projects/Jarvis-OS/memory/vault/coder"
    read_only: false
    scope: "department"
    index_file: "_index.json"

# ============================================================
# PERSONAL PROFILE
# ============================================================
personal:
  external_memory:
    provider: "obsidian"
    vault_path: "/home/g0pi/Projects/Jarvis-OS/memory/vault/personal"
    read_only: false
    scope: "department"
    index_file: "_index.json"

# ============================================================
# FINANCE PROFILE
# ============================================================
finance:
  external_memory:
    provider: "obsidian"
    vault_path: "/home/g0pi/Projects/Jarvis-OS/memory/vault/finance"
    read_only: false
    scope: "department"
    index_file: "_index.json"

# ============================================================
# CREATIVE PROFILE
# ============================================================
creative:
  external_memory:
    provider: "obsidian"
    vault_path: "/home/g0pi/Projects/Jarvis-OS/memory/vault/creative"
    read_only: false
    scope: "department"
    index_file: "_index.json"

# ============================================================
# OPS PROFILE
# ============================================================
ops:
  external_memory:
    provider: "obsidian"
    vault_path: "/home/g0pi/Projects/Jarvis-OS/memory/vault/ops"
    read_only: false
    scope: "department"
    index_file: "_index.json"

# ============================================================
# DEFAULT PROFILE (fallback)
# ============================================================
default:
  external_memory:
    provider: "obsidian"
    vault_path: "/home/g0pi/Projects/Jarvis-OS/memory/vault/default"
    read_only: false
    scope: "department"
    index_file: "_index.json"